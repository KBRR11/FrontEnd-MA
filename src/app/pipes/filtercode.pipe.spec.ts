import { FiltercodePipe } from './filtercode.pipe';

describe('FiltercodePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercodePipe();
    expect(pipe).toBeTruthy();
  });
});
