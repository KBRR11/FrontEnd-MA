import { FilterdocumentPipe } from './filterdocument.pipe';

describe('FilterdocumentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterdocumentPipe();
    expect(pipe).toBeTruthy();
  });
});
